# @vulse/renderer

Vue 3 field renderers used by Vulse blueprints in both the admin SPA and
SSR site.

```sh
pnpm add @vulse/renderer
```

Ships a `./styles` export for the renderer CSS. Imported automatically by
`@vulse/admin` and `@vulse/site`.
